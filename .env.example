# Only needed if you wire MCP servers. Nothing in this repo requires them.
# Never commit the filled-in version. .env is gitignored.

# Ads platform, READ SCOPE ONLY. Seat 10 never writes.
META_ACCESS_TOKEN=
META_AD_ACCOUNT_ID=act_XXXXXXXXXX

# Anything else your chosen MCP servers need.
NOTION_API_KEY=
SLACK_BOT_TOKEN=
